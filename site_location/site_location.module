<?php

/**
 * @file
 * This Module has the feature to show the current time.
 *
 * Based on the location config the current time will change.
 */

/**
 * Implements hook_theme().
 */
function site_location_theme($existing, $type, $theme, $path) {
  return [
    'location_data' => [
      'variables' => ['time_data' => [], 'title' => ''],
    ],
  ];
}
